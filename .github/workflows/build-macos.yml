name: Build macOS (.app + zip)

on:
  workflow_dispatch:
  push:
    branches: ["main"]

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build macOS App
        run: |
          set -e
          # Si tu as un .spec, on l'utilise. Sinon on tente main.py.
          if [ -f "ResponseReportGenerator.spec" ]; then
            pyinstaller --noconfirm --clean ResponseReportGenerator.spec
          else
                  - name: Build macOS App
        run: |
          pyinstaller --noconfirm --clean ResponseReportGenerator-mac.spec
          echo "=== dist content ==="
          ls -la dist || true
          find dist -maxdepth 6 -name "*.app" -print || true


          echo "=== dist content ==="
          ls -la dist || true
          echo "=== find .app ==="
          find dist -maxdepth 4 -name "*.app" -print || true

      - name: Zip the .app
        run: |
          set -e
          APP_PATH="$(find dist -maxdepth 4 -name '*.app' -print -quit)"
          echo "APP_PATH=$APP_PATH"
          if [ -z "$APP_PATH" ]; then
            echo "ERROR: No .app found in dist/. PyInstaller did not create an app bundle."
            echo "dist listing:"
            ls -la dist || true
            exit 1
          fi
          ditto -c -k --sequesterRsrc --keepParent "$APP_PATH" ResponseReportGenerator-macOS.zip
          ls -la ResponseReportGenerator-macOS.zip

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: ResponseReportGenerator-macOS
          path: ResponseReportGenerator-macOS.zip
